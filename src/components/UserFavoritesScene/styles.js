import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listView: {
  },
  moreButtonContainer: {
    width: '100%',
    alignItems: 'center',
    paddingBottom: 10,
  },
});
