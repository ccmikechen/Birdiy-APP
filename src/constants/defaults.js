export const DEFAULT_MATERIAL = Object.freeze({ name: '', amountUnit: '', url: '' });
export const DEFAULT_FILE = Object.freeze({ type: 'link', name: '', url: '' });
export const DEFAULT_METHOD = Object.freeze({ image: null, title: '', content: '' });
export const DEFAULT_PROJECT_BATCH_LOAD = 10;
export const DEFAULT_POST_BATCH_LOAD = 5;
export const DEFAULT_PROFILE = Object.freeze({
  name: 'Guest',
  image: null,
  followerCount: 0,
  followingCount: 0,
});
